{
  "name": "@flavorpress/wordpress-plugin",
  "version": "0.1.0",
  "description": "Local WordPress plugin bundle for FlavorPress Content Analyzer.",
  "private": true,
  "type": "module",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && vite build --config vite.config.ts"
  },
  "dependencies": {
    "@flavorpress/analyzer-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.4.2"
  }
}
